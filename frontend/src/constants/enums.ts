/**
 * Vehicle Enum
 * Key indicates the type
 * Value indicates the i18n key for the text to be displayed.
 */
export enum Vehicle {
    POWER_UNIT,
    TRAILER
}

export enum Vehicle2 {
    XYZ, SSA
}